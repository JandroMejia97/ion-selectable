import { SelectableValueAccessorDirective } from './selectable-value-accessor.directive';

describe('SelectableValueAccessorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectableValueAccessorDirective();
    expect(directive).toBeTruthy();
  });
});
