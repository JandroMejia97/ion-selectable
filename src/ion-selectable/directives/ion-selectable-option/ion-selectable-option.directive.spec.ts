import { IonSelectableOptionDirective } from './ion-selectable-option.directive';

describe('IonSelectableOption', () => {
  it('should create an instance', () => {
    const directive = new IonSelectableOptionDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
